import { IUser, IUserCreateDTO, IUserLoginDTO, IUserProvider } from "./users";
import { ITank, ITankCreateDTO, ITankProvider } from "./tanks";

export {
  IUser,
  IUserCreateDTO,
  IUserLoginDTO,
  IUserProvider,
  ITank,
  ITankCreateDTO,
  ITankProvider,
};
